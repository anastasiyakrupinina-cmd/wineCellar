name: wine_cellar
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  #navigation
  auto_route: ^11.1.0
  google_fonts: ^8.1.0
  #storage
  shared_preferences: ^2.5.5
  get_it: ^9.2.1
  injectable: ^2.7.1
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2
  # state management provider between repository and ui layer
  flutter_bloc: ^9.1.1
  dio: ^5.9.2
  flutter_staggered_grid_view: ^0.7.0

  #local database
  sqflite: ^2.3.3
  sqflite_common_ffi: ^2.3.4
  sqlite3_flutter_libs: ^0.5.0
  path: ^1.9.0
  path_provider: ^2.1.5

  #cloud sync
  webdav_client: ^1.2.2
  flutter_secure_storage: ^9.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  auto_route_generator: ^10.5.0
  injectable_generator: ^2.12.0
  build_runner: ^2.4.0
  flutter_launcher_icons: ^0.14.3

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/icon/app_icon.png"
  min_sdk_android: 21
  remove_alpha_ios: true
  web:
    generate: false
  windows:
    generate: true
    image_path: "assets/icon/app_icon.png"
  macos:
    generate: false


flutter:
  generate: true
  uses-material-design: true



## packaege =>package generator => build_runner
## auto_route=>auto_route_generator=> build_runner
## dart run build_runner build --delete-conflicting-outputs

## create flutter project 
##  pacjeges внури папка pubspec.yaml
##  firebase projext 
## https://console.firebase.google.com/ => create project => add app => follow steps

## FirebaseAuth => email and password auth => enable
## FirebaseDatabase => create database => start in test mode
## navigation => auto_route
## resources=> colors, text styles, dependency injection => get_it, injectable, storage => shared_preferences, theme, widgets, vlaidators



## NameFeature_page
## data and presentation
## data => model, repository
## presentation => cubit, page, widget (local widgets)


## model =>reposiotry=>cubit(provider)=>ui(page)

